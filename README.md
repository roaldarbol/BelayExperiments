# BelayExperiments
Micropython code for experiments. Uses Pixi to install Belay. Experiments are in the `experiments` folder, and devices are specified in the `experiments/devices` folder.

## Get started

1. Install Pixi
2. Clone repository
3. `pixi shell`
4. Run script